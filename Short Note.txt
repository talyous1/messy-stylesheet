The most useful refactor I did was combine multiple copies of the same code into one block, and then combine classes that have the same styles exact styles applied. This simplifies the code and makes it shorter. Organizing CSS into multiple layers makes it way easier to understand and navigate. 